# dependencies
node_modules
.pnpm-store

# next.js
.next
out
next-env.d.ts

# production
build
dist

# environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editors
.vscode
.idea
.DS_Store
Thumbs.db

# typescript
*.tsbuildinfo

# vercel
.vercel

# claude code local settings
.claude/

# supabase
supabase/.branches
supabase/.temp
# Locally extracted supabase CLI binaries (do not commit)
/supabase.exe
/supabase-go.exe

# misc
coverage
.nyc_output

.vercel

# signing keys (Tauri updater minisign 等の秘密鍵を絶対にコミットしない)
*.key
# シェルが $HOME を未展開のまま作る誤フォルダ(鍵が紛れ込みうる)
$HOME/

# スマホ(PWA)版 SPA(scripts/build-mobile-app.mjs が生成)
/public/app/
