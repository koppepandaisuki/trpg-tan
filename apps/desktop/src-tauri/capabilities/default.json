{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "メインウィンドウの既定権限。今は最小(core のみ)。ファイル保存等は後でプラグインと権限を追加する。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
