# pnpm ワークスペース定義(その場ワークスペース方式)。
#
# - Web アプリ(Next.js)はリポジトリ "ルート" のパッケージのまま据え置き。
#   ここには列挙しない(ルート自身がワークスペースのルートになる)。
# - 共有コードは packages/*、デスクトップ等のアプリは apps/* に置く。
#
# これにより既存 Web の import パスを 1 つも壊さず、
# trpg-core(型・ダイス・システム定義)を Web/アプリ双方から参照できる。
packages:
  - "packages/*"
  - "apps/*"
